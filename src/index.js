import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    Welcome to the new project! We wish you success in achieving it!
  </React.StrictMode>
);
